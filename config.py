# IMAGE_DIR = "images/all"
# IMAGE_DIR = "images/test_images"
# another directory for newer images to compensate for 1 cent and 1 ringgit coins
IMAGE_DIR = "images/1_sen_images"

# limiting to 150 per class, assuming only have 1, 10, 20, 50 cents, and 1 ringgit coins
IMAGE_LIMIT = 750

# text file to store the URLs scraped using PyImageSearch Javascript method
URL_FILE = "data/1_sen_urls.txt"

# to run the download asynchronously or not
ASYNC = True
